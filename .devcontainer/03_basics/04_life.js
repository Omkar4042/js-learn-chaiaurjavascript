// Immediately Invoked function Expression (IIFE)
// HERE to be secure from the polution of global scope we use IIFE here we wrape the whole function in ()and immediately after a () its function call  , like how we call normal function () in same way 
// (function chai (){
//     console.log(`DB CONNECTED`);

// })(); now here after IIFE function call ; 
// is mandatory as iife does not know where to stope the immeditale execution so ; is need 
// output : DB CONNECTED

// iife using arrow function 
// named iife
// ((name)=>{
//     console.log(`DB connected  two`);
// })('hitesh');
